# MAMP default MySQL credentials and port on macOS.
# Copy these values into .env.local when using MAMP.
DATABASE_URL="mysql://root:root@127.0.0.1:8889/barattolo?serverVersion=8.0&charset=utf8mb4"

# This value is useful for commands that generate URLs outside an HTTP request.
DEFAULT_URI=http://localhost:8888
