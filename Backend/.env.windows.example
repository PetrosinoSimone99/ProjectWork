# Laragon and XAMPP commonly use MySQL on port 3306 with the root user and no password.
# Copy these values into .env.local and change them if the local installation differs.
DATABASE_URL="mysql://root:@127.0.0.1:3306/barattolo?serverVersion=8.0&charset=utf8mb4"
DEFAULT_URI=http://localhost
